---
name: Use pnpm instead of npm
description: User prefers pnpm for package management in this project
type: feedback
---

Always use `pnpm` instead of `npm` for installing packages or running scripts in this project.

**Why:** User explicitly corrected npm usage and asked for pnpm.

**How to apply:** Replace `npm install` with `pnpm add`, `npm run` with `pnpm`, etc.
